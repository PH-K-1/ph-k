<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Phk" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 상태바 색상 (흰색) -->
        <item name="android:statusBarColor">@color/white</item>
        <!-- 상태바 텍스트 및 아이콘 색상 (어두운 색) -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- 기본 테마를 부모로 하는 실제 테마 -->
    <style name="Theme.Phk" parent="Base.Theme.Phk" />
</resources>
